spring:
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        namespace: service_dev
        group: SERVICE_DEV_GROUP
      config:
        server-addr: ${spring.cloud.nacos.discovery.server-addr}
        namespace: service_dev
        group: SERVICE_DEV_GROUP
        file-extension: yaml #properties  yaml
        prefix: user
        refresh-enabled: true
